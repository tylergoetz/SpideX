function Controls(){


document.addEventListener('keydown', (event) => {
    const keyName = event.key;
    switch(keyName){
        case 'g':

            break;
        case 'f':

            break;
        case 'h':

            break;
    }
    
}, false);

}